import { ScreensNavigator } from "../modules/ScreensNavigator";

export const screensNavigator = new ScreensNavigator({
  SPLASH: 0,
  HOME: 1,
});

export const NAV_VALUES = screensNavigator.NAV_VALUES;
